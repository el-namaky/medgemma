# دليل تشغيل Med-Gemma على Google Colab

هذا الدليل يشرح كيفية تشغيل نظام Med-Gemma على بيئة Google Colab السحابية للاستفادة من كروت الشاشة القوية (GPU) مجاناً.
لديك خياران للتشغيل:
1. **عن طريق Google Drive** (للملفات المحلية).
2. **عن طريق GitHub** (الأسهل والأسرع).

## المتطلبات المسبقة
1. حساب Google (Gmail).
2. حساب على [Hugging Face](https://huggingface.co/).
3. الموافقة على شروط استخدام موديل Gemma (إذا لزم الأمر) وإنشاء Access Token.

---

## 🟢 الخيار 1: التشغيل عن طريق GitHub (موصى به)

هذه الطريقة أسهل لأنها لا تتطلب رفع الملفات يدوياً إلى Drive.

### 1. تجهيز الكود
- تأكد أن الكود الخاص بك موجود على مستودع (Repository) في GitHub.
- إذا كان المستودع خاص (Private)، ستحتاج لإنشاء **Personal Access Token** من إعدادات GitHub.

### 2. فتح Notebook
1. ارفع ملف `medgemma_github.ipynb` إلى Google Colab أو افتحه مباشرة.
2. يمكنك فتحه من هذا الرابط (بعد رفع الملف إلى GitHub):
   `https://colab.research.google.com/github/USERNAME/REPO_NAME/blob/main/medgemma_github.ipynb`

### 3. إعداد البيئة
1. من القائمة العلوية في Colab، اختر `Runtime` > `Change runtime type`.
2. اختر `T4 GPU` من خيارات Hardware accelerator.
3. اضغط `Save`.

### 4. تشغيل التطبيق
1. **Clone Repository**: ضع رابط المستودع الخاص بك في الخلية المخصصة وشغلها.
2. **Install Requirements**: سيقوم بتحميل المكتبات.
3. **Hugging Face Login**: أدخل التوكن.
4. **Run App**: شغل التطبيق واضغط على الرابط `gradio.live`.

---

## 🔵 الخيار 2: التشغيل عن طريق Google Drive

استخدم هذه الطريقة إذا كنت تريد العمل على ملفاتك الخاصة في Drive دون رفعها لـ GitHub.

### 1. تجهيز الملفات
1. ارفع مجلد المشروع (`medgemma`) بالكامل إلى **My Drive** في Google Drive الخاص بك.
   - يفضل أن يكون المسار: `My Drive/MedGemma`.

### 2. فتح Notebook
1. اذهب لمجلد المشروع في Google Drive.
2. انقر بـ **يمين الفأرة** على ملف `medgemma_colab.ipynb`.
3. اختر **Open with** (فتح باستخدام) > **Google Colaboratory**.

### 3. إعداد البيئة (Runtime)
- (نفس خطوات الخيار 1: تفعيل `T4 GPU`).

### 4. تشغيل الخلايا
1. **Mount Drive**: ستربط Colab بملفاتك على درايف.
2. **Setup Path**: تأكد أن `PROJECT_PATH` يشير لمكان المجلد الصحيح.
3. **Run Remainder**: أكمل باقي الخلايا كما هو موضح في الدفتر.

---

## مشاكل شائعة
- **Private Repo Error**: إذا فشل استنساخ المستودع الخاص، تأكد من استخدام التوكن في الرابط:
  `https://TOKEN@github.com/user/repo.git`
- **Out of Memory (OOM)**: إذا توقف التشغيل بسبب الذاكرة، أعد تشغيل Runtime.
- **Model not found**: تأكد من قبولك لشروط Gemma على Hugging Face وأن التوكن صحيح.
