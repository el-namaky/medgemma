# 🏥 دليل مشروع Gemma-Health Sentinel 

هذا الملف يشرح تفاصيل المشروع، كيفية تشغيله، وكيفية التقديم للهاكثون.

---

## 📂 هيكلية المشروع (Project Structure)

| المجلد/الملف | الوظيفة |
| :--- | :--- |
| `app.py` | **الملف الرئيسي:** نقطة الانطلاق لتشغيل التطبيق الكامل بواجهة Gradio. |
| `notebook.ipynb` | **مفكرة تفاعلية:** لتجربة الأكواد واختبار نماذج الذكاء الاصطناعي بشكل تجريبي. |
| `ai/` | يحتوي على منطق الذكاء الاصطناعي، التعامل مع نموذج MedGemma، والـ Prompts. |
| `db/` | مسؤول عن قاعدة البيانات (SQLite)، تهيئتها (`init_db.py`) وإضافة بيانات تجريبية (`seed_data.py`). |
| `ui/` | يحتوي على تصميم الواجهات الثلاث (الاستقبال، الطوارئ، التشخيص). |
| `utils/` | أدوات مساعدة عامة تستخدم في مختلف أنحاء المشروع. |
| `requirements.txt` | قائمة المكتبات البرمجية اللازمة لتشغيل المشروع. |
| `test_app.py` | ملف الاختبارات للتأكد من سلامة النظام قبل التشغيل أو الرفع. |

---

## 🎨 كيف ستظهر الواجهات (UI flow)

التطبيق مقسم إلى **3 مراحل رئيسية** تظهر كتبويبات (Tabs) في المتصفح:

1. **🚪 الاستقبال (Reception):** لتسجيل بيانات المريض والحصول على ملخص أولي لحالته.
2. **🚨 الطوارئ (Emergency):** واجهة مخصصة للطبيب للتعامل مع الحالة فوراً، مع تنبيهات ذكية لتفاعلات الأدوية.
3. **🔍 التشخيص المعمق (Diagnosis):** مرحلة التحليل المتقدم للبحث عن الأسباب الخفية للأعراض المعقدة.

---

## 🚀 إعدادات ما قبل التشغيل لأول مرة

قبل تشغيل المشروع لأول مرة، تأكد من القيام بالآتي:

1. **تثبيت المكتبات:**
   ```bash
   pip install -r requirements.txt
   ```
2. **تهيئية البيانات:**
   عند تشغيل `app.py` لأول مرة، سيقوم النظام تلقائياً بإنشاء ملف `hospital.db` وإضافة البيانات التجريبية.

---

## 🧪 كيف تختبر المشروع

يمكنك اختبار المكونات الأساسية (قاعدة البيانات، الاستعلامات، الذكاء الاصطناعي) عبر تشغيل ملف الاختبار:
```bash
python test_app.py
```
إذا انتهى التشغيل بكلمة `✅ All tests passed!` فهذا يعني أن مشروعك جاهز تماماً.

---

## 🏆 ماذا ترفع على الهاكثون؟

عند التقديم، يفضل رفع المجلد كاملاً مع استثناء الملفات المؤقتة. الملفات الأساسية هي:
1. مجلدات الكود: `ai/`, `db/`, `ui/`, `utils/`.
2. الملفات الرئيسية: `app.py`, `requirements.txt`, `test_app.py`.
3. التوثيق: `README.md` وهذا الدليل `HACKATHON_GUIDE.md`.
4. **تجنب رفع:** مجلدات `__pycache__` أو ملفات البيئة الافتراضية `.venv`.

---

## 🆚 الفرق بين التشغيل عبر `app.py` و `notebook.ipynb`

يوفر المشروع طريقتين للتشغيل، ولكل منهما استخدام محدد:

| وجه المقارنة | تشغيل `python app.py` | تشغيل `notebook.ipynb` |
| :--- | :--- | :--- |
| **التجربة** | تجربة مستخدم كاملة ونهائية. | تجربة تعليمية وتجريبية خطوة بخطوة. |
| **الواجهة** | يجمع الثلاث واجهات في تبويبات (Tabs) واحدة. | يشغل كل واجهة بشكل منفصل في خلية خاصة. |
| **الاستخدام** | للتقديم النهائي في الهاكثون وللاستخدام الفعلي. | للتطوير، واكتشاف الأخطاء، وفهم منطق الكود. |
| **البيئة** | يعمل في نافذة متصفح مستقلة. | يعمل داخل بيئة VS Code أو Colab. |

---

## 📓 شرح مفكرة الـ Notebook (`notebook.ipynb`)

هذه المفكرة مخصصة للتجارب السريعة واختبار المكونات بشكل منفصل:

| الخلية | وظيفتها |
| :--- | :--- |
| **Cell 1** | تثبيت المكتبات (Dependencies). |
| **Cell 2** | تهيئة قاعدة البيانات وإضافة مرضى تجريبيين. |
| **Cell 3** | تحميل نموذج MedGemma واختبار قدرات التلخيص. |
| **Cell 4-6** | تشغيل واجهات (الاستقبال، الطوارئ، التشخيص) كلاً على حدة. |
| **Cell 7** | تشغيل سيناريو كامل (قصة المريضة سارة) برمجياً. |

### 🔍 كيف تعرف حالة تنفيذ الخلية؟

*   **النجاح (Success):** تظهر علامة **صح خضراء** بجانب الخلية مع وقت التنفيذ (مثل `1.2s`).
*   **الفشل (Failure):** تظهر علامة **(X) حمراء** مع رسالة خطأ تشرح المشكلة أسفل الخلية.
*   **قيد التنفيذ:** تظهر علامة "دائرة تدور" أو نجمة `[*]` مما يعني أن الخلية ما زالت تعمل.

---

## ☁️ كيف تشغل المشروع على Google Colab

لتشغيل المشروع على كولاب، اتبع هذه الخطوات:

1. **رفع الملفات:** ارفع مجلد المشروع كاملاً إلى Colab أو اربطه بـ Google Drive.
2. **تثبيت المكتبات:** في خلية كود (Code Cell)، نفذ الأمر:
   ```python
   !pip install -r requirements.txt
   ```
3. **تعديل بسيط للتشغيل:** لأن كولاب يحتاج لـ Public URL لرؤية واجهة Gradio، افتح ملف `app.py` وغير سطور الـ `launch` لتصبح هكذا:
   ```python
   app.launch(share=True)
   ```
4. **التشغيل:** نفذ الأمر التالي في خلية كود:
   ```python
   !python app.py
   ```
5. **الرابط:** سيظهر لك رابط ينتهي بـ `.gradio.live` اضغط عليه لتفتح الواجهة.

---




لقد قمت بتحديث الدليل ليشمل تعليمات التشغيل لأول مرة وتعليمات خاصة بـ Google Colab. إليك الملخص:

🚀 التشغيل لأول مرة (على جهازك):
افتح التيرمينال في مجلد المشروع ونفذ:
bash
pip install -r requirements.txt
ابدأ التشغيل بالامر:
bash
python app.py
☁️ التشغيل على Google Colab:
ارفع مجلد المشروع لـ Colab.
ثبت المكتبات بنفس الأمر أعلاه (مع إضافة ! في البداية).
هام: في ملف app.py تأكد من تغيير سطر التشغيل إلى app.launch(share=True) لتفعيل رابط الـ Gradio العام.




--------
> [!IMPORTANT]
> نظام **Gemma-Health Sentinel** هو أداة دعم قرار سريري ومساعد للطبيب، وليس بديلاً عن التشخيص الطبي البشري.


-----------------


